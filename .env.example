# ─── Environment Variables ─────────────────────────────────────────────────
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to Git — it is already in .gitignore.
#
# ⚠️ SECURITY: The old Gemini API key (AIzaSyD5nCDzZRl-V_c0NAuz67OWzm6gDqthOyo)
# was exposed in the original main.js. REVOKE IT immediately at:
# https://console.cloud.google.com/apis/credentials
# Then generate a NEW key and paste it below.

# ── Gemini AI Chatbot ────────────────────────────────────────────────────────
# Used by: app/api/chat/route.ts (server-side only — never sent to the browser)
# How to get: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_new_gemini_api_key_here

# ── Contact Form (optional — only if using Resend for email) ─────────────────
# RESEND_API_KEY=your_resend_api_key_here
# CONTACT_EMAIL_TO=huzaifa.fullstack@gmail.com

# ── Site URL (for absolute URLs in metadata) ─────────────────────────────────
NEXT_PUBLIC_SITE_URL=https://muhammadhuzaifa.dev
